// =====================================================================
// TAILWIND CSS — КОНФИГУРАЦИЯ СБОРКИ (3.4.17 — та же версия, что была у CDN)
// =====================================================================
// Раньше Tailwind подключался как Play CDN (`cdn.tailwindcss.com/3.4.17`):
// браузер сам разбирал разметку и генерировал стили. Минусы: внешняя
// зависимость (в офлайне стилей нет), SRI-проверку поставить нельзя, лишняя
// работа на каждом устройстве, CSP вынужден разрешать чужой домен.
// Теперь стили собираются заранее:
//
//     npm run build          → css/tailwind.css (минифицированный)
//
// `content` — что сканировать на имена классов. Сканируются файлы целиком
// (а не только атрибуты `class`), поэтому классы из шаблонных строк модулей
// попадают в сборку.
//
// ВАЖНО: класс, собранный склейкой (`bg-${tone}-100`), в сборку НЕ попадёт —
// сборка статическая, в отличие от Play CDN. Такие места пишутся целыми
// строками (см. js/modules/dashboard.js — у групп задач поле `badge`), либо
// заносятся в `safelist` ниже.
// =====================================================================

/** @type {import('tailwindcss').Config} */
export default {
    content: [
        './index.html',
        './js/**/*.js'
    ],
    safelist: [],
    theme: {
        extend: {}
    },
    plugins: []
};
