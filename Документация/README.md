# Документация RSK Odessa

Готовые к печати инструкции для сотрудников: как установить приложение, как им пользоваться
и что делает каждая должность. В папке лежат и **PDF** (их и отправляют людям), и **HTML-исходники**
(из них PDF собираются одной командой).

## Что внутри

| Файл | Кому | О чём |
| --- | --- | --- |
| `Инструкция-01-Установка-приложения.pdf` | всем сотрудникам | Установка как приложения (PWA) на Android, iPhone/iPad, Windows, macOS; обновление версии; работа без интернета; если не устанавливается; удаление и переустановка |
| `Инструкция-02-Начало-работы.pdf` | всем сотрудникам | Вход и регистрация, шапка и разделы, кабинет (баланс, авансовый отчёт, мои заявки и задачи), статусы, общие правила, кто что видит, частые вопросы |
| `Инструкция-03-Администратор.pdf` | Администратор | Выдача доступов, карточки сотрудников (блокировка, восстановление, привязка аккаунта), касса и финансовые заявки, объекты и смета, права на двух уровнях (UI + RLS), чек-листы |
| `Инструкция-04-Директор.pdf` | Директор | Цифры и план-факт по всем объектам, контроль заявок и долгов, согласование и выдача денег, реестр и выгрузки, постановка задач |
| `Инструкция-05-Главный-инженер.pdf` | Главный инженер | Смета, график работ, документация, план-факт и доп. расходы, заявки на материалы, касса, задачи, реестр |
| `Инструкция-06-Прораб.pdf` | Прораб | Свои объекты: задачи с фотоотчётом, заявки на материалы и деньги, подотчёт и авансовый отчёт, закрытие разделов графика |
| `Инструкция-07-Снабженец.pdf` | Снабженец | Рабочий экран заявок: взять в работу → загрузить счёт поставщика → доставить на объект с ценами и поставщиком → архив; оплата фирмой или из подотчёта; статус оплаты и передача долга финансисту; реестр |
| `Инструкция-08-Инженер-ПТО.pdf` | Инженер ПТО | Смета (подготовка файла, загрузка, обновление), график, документация, план-факт и доп. расходы, заявки, задачи, реестр |
| `Инструкция-09-Финансист.pdf` | Финансист | «💼 Рабочий стол»: баланс своего подотчёта, выдача денег по одобренным заявкам, счета поставщиков (оплата безналом фирмы), ведомость пополнений, свой язык и тема, чек-листы |

HTML-файлы называются так же (с расширением `.html`) — их удобно открыть в браузере, чтобы
прочитать с телефона или отправить ссылкой.

## Что изменилось в v2.4.0 (обновление документации)

| Файл | Что добавлено |
| --- | --- |
| `Инструкция-09-Финансист` | **Новый документ** под должность «Финансист» (роль появилась в системе: выдача денег по одобренным заявкам и оплата счетов поставщиков) |
| `Инструкция-02-Начало-работы` | Раздел «⚙ Настройки: язык и цветовая схема», статус «🚚 Доставлено» и статус оплаты («⏳ Ожидает оплаты» / «✅ Оплачено»), должность «Финансист» в матрице ролей |
| `Инструкция-03-Администратор` | Должностей стало семь (+ Финансист), описан его рабочий стол; порядок обработки заявки снабжения: взять в работу → счёт поставщика → доставлено на объект; **новый раздел «9. Обновление базы данных (миграции)»** — как применить `database/migrate-v2.4.sql` и что видно в приложении, если база не обновлена. Раздел дополнен описанием «наполовину применённой» миграции: почему SQL Editor останавливается на первой ошибке, что теперь показывают самопроверка миграции и вкладка **Notices**, и что видно сотрудникам в «📦 Снабжении», «📊 Реестре» и в блоке «🧾 Счета на материалы» |
| `Инструкция-04-Директор` | Подотчёт финансиста: «💼 Пополнить баланс финансиста» и «📄 Ведомость пополнений»; блок «🧾 Счета на материалы»; актуальные статусы заявок |
| `Инструкция-07-Снабженец` | Полностью обновлён рабочий поток: «🧾 Счёт от поставщика» → «🚚 Доставлено на объект» → архив, статусы оплаты, передача долга финансисту. В окне счёта появился выбор **«🚚 Чья доставка»**: 🏬 доставка поставщика входит в счёт, 🏢 доставка компании — вне счёта (строка «🚚 Доставка компании» в заявке и реестре) |
| `Инструкция-09-Финансист` | Оплата счетов поставщиков дополнена разделом про доставку: в очереди «🧾 Счета на материалы» сумма — это то, что выставил поставщик (доставка компании в неё не входит и долгом не считается; в «📊 Реестре материалов» у такой строки «🏢 Вне счёта») |
| `Инструкция-01`, `05`, `06`, `08` | Формулировки о «закрытой заявке» заменены на «доставлено на объект» (учёт начинается с доставки материалов) |
| `Инструкция-01-Установка-приложения` | Раздел «7. Проверка, что приложение установилось»: в *Cache Storage* теперь указан настоящий кэш `rsk-odessa-v2.4.0-r6` и объяснено правило имени (`rsk-odessa-v<версия>-<ревизия>` из `sw.js`). Раньше там был кэш `rsk-odessa-v2.1.0` — администратор искал его и не находил |

Во всех документах версия в колонтитуле поднята до **v2.4.0**.

> ⚠️ `Презентация.pdf` в корне репозитория пока описывает версию v2.1.0: в ней нет роли
> «Финансист» и цепочки «счёт → доставка → оплата». Презентация собирается отдельно
> (`Презентация.html`), её нужно переверстать под v2.4.0 — это не автоматическое обновление,
> а переработка слайдов.

## Пересобрать PDF

PDF собираются headless-браузером из HTML: `@page { size: A4 portrait }` и правила
`@media print` лежат в `docs.css`, поэтому дополнительно ничего настраивать не нужно.

```powershell
# из корня репозитория
powershell -ExecutionPolicy Bypass -File tools/make-docs.ps1
```

Скрипт берёт все `*.html` в этой папке, печатает каждый в одноимённый `.pdf` и пишет в консоль
размер результата. Нужен установленный Chrome или Edge.

Вручную то же самое:

```powershell
& "$env:ProgramFiles\Google\Chrome\Application\chrome.exe" --headless=new `
    --no-pdf-header-footer --virtual-time-budget=6000 `
    "--print-to-pdf=$PWD\Документация\Инструкция-06-Прораб.pdf" `
    "file:///$($PWD.Path -replace '\\','/')/Документация/Инструкция-06-Прораб.html"
```

## Правила оформления

* `docs.css` — общий стиль всех документов, включая печатный. Новые документы подключают его
  строкой `<link rel="stylesheet" href="docs.css">` и вставлять свои стили не должны.
* Логотип берётся из корня репозитория (`../logo.png`), поэтому папку можно переносить целиком
  и открывать даже по `file://`.
* Структура документа: `header.cover` (логотип, надзаголовок, название, лид, «чипы»),
  далее `section.sec` с `h2` (нумерованный раздел) — так PDF собирается в аккуратные страницы.
* Готовые блоки: `.cards` / `.card`, `.steps` (нумерованные шаги), `.note` / `.warn` / `.ok` /
  `.danger` (врезки), `.pills` (статусы), `.check` / `.cross` (чек-листы), `table` (таблицы).

## Важно: это внутренние документы

Инструкции описывают роли, права и порядок работы с деньгами — в интернет их выкладывать нельзя.
Вместе с `Презентация.html` / `Презентация.pdf` они перечислены в `.vercelignore` в корне репозитория,
поэтому при деплое приложения на Vercel эти файлы не попадают на публичный хостинг.
PDF передавайте сотрудникам напрямую (почта, мессенджер, папка на диске).

При изменении прав ролей (`js/permissions.js`) инструкции нужно обновить: матрица доступа описана
в «Инструкция-02-Начало-работы» и в файлах по должностям.
